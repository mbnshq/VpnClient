<UserControl x:Class="NovaVpn.App.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Logs" FontSize="20" FontWeight="SemiBold" />

        <DockPanel Grid.Row="1" Margin="0,12">
            <Button Content="Refresh" Command="{Binding RefreshCommand}" DockPanel.Dock="Right"
                    Margin="8,0,0,0" />
            <TextBox Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Filter" />
        </DockPanel>

        <Border Grid.Row="2" BorderThickness="1"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                CornerRadius="8" Padding="8"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}">
            <ListBox ItemsSource="{Binding Lines}" BorderThickness="0" Background="Transparent"
                     FontFamily="Cascadia Mono, Consolas" FontSize="12"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto" />
        </Border>

        <TextBlock Grid.Row="3" Text="{Binding Status}" Opacity="0.6" Margin="0,8,0,0" />
    </Grid>
</UserControl>
